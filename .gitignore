# --- Development artefacts (repo root only; leading slash anchors to root) ---
/vendor/
/build/
/dist/
composer.phar
.phpunit.result.cache
.phpcs.cache
.phpstan.cache/

# NOTE: the plugin's runtime autoloader is the hand-written
# bunnify-frontend/autoload.php — no Composer vendor dir ships.

# --- Local-only AI working context (cross-project prompts/status; never shipped) ---
docs/00-ai-context/

# --- Editors / IDEs ---
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- OS cruft ---
.DS_Store
Thumbs.db
.directory
._*
~*
*~

# --- Version control noise ---
.svn
.cvs

# --- Backups / logs / archives ---
*.bak
*.swp
*.orig
*.rej
*.log
*.diff
*.err
*.db
.htaccess
