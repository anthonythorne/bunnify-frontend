# --- Development artefacts (repo root only; leading slash anchors to root) ---
/vendor/
/build/
/dist/
composer.phar
.phpunit.result.cache
.phpcs.cache
.phpstan.cache/

# NOTE: bunnify-frontend/build-tools/vendor/ is intentionally NOT ignored —
# it is the plugin's minimal runtime autoloader and ships with the plugin.

# --- Editors / IDEs ---
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- OS cruft ---
.DS_Store
Thumbs.db
.directory
._*
~*
*~

# --- Version control noise ---
.svn
.cvs

# --- Backups / logs / archives ---
*.bak
*.swp
*.orig
*.rej
*.log
*.diff
*.err
*.db
.htaccess
